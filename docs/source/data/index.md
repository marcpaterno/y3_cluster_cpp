# Data and calibration files

Inventory of every external file in the repository's `data/` tree and of
the out-of-repo inputs the reference pipeline needs.
**Goal: no unexplained data file enters the likelihood.**

## Files required by the reference pipeline

Only a handful of files are opened at runtime by
`mock_mcmc_cp_camb.ini`:

| File | Quantity | Used by | Origin |
|---|---|---|---|
| `camb-emulator/camb-for-cp/models/camb_linear_s8_v3c_emulator.npz` (+ `_nonu_` variant) | linear $P(k,z)$ emulators (σ₈ amplitude) | `cp_camb` | trained in the `camb-emulator` repo against CAMB |
| `data/mock/mock_dv_cp_camb.npz` (smoke) / `des-cluster-nersc/data/mock/mock_dv_widePlanck_jkcov.npz` (production) | mock data vector + inverse covariances | `likelihoods` | mock-DV scripts in `des-cluster-nersc` — **`data/mock/` is not shipped in this tree and must be generated before a run** |
| `data/test_cluster_Y1.fits` (HDU 6) | source $n(z)$ (`z_mid`, `bin1`) | `average_sigma_crit_inv` (bypassed under `unity = T`) | DES Y1 catalog product |
| `data/nfw_off_center/table_1000_…_gamma_*.txt` | γ-kernel miscentered NFW $\Delta\Sigma_{\rm mis}$ lookup (1000×1000, log axes) | `Shear1hMisSel` | precomputed convolution tables |
| `data/nfw_off_center/table_1000_…_single_*.txt` | single-offset (δ-kernel) $\Sigma_{\rm mis}$/$\Delta\Sigma_{\rm mis}$ lookups | `shear_prj` | precomputed convolution tables |
| `y3_buzzard/prj_params.py` (embedded table) | calibrated EMG projection coefficients (8 coefficients × 15-node z grid, 0.10–0.80) | `sel_function`, `bsel` | Costanzi-2026 calibration (the on-disk `data/prj_params/` is empty — the table lives in code) |
| bin definitions (λ edges 20/30/45/60/200; z edges 0.20/0.35/0.50/0.65; 10 radii) | ini vectors | all wall-grid modules | analysis choice |
| miscentering $(f_{\rm mis}, \tau_{\rm mis})$ | DataBlock `miscentering` section or in-code defaults (0.22, 0.17) | `Shear1hMisSel` | DES-Y3 redMaPPer calibration (Kelly et al. 2023) |

```{todo}
Add Units and "generated how" columns for the pipeline inputs, and
verify the list against a fresh smoke run.
```

## Inventory

The `data/` tree mixes three kinds of content: files opened at runtime by
the reference `mock_mcmc_cp_camb` pipeline (*pipeline input*), files read
or regenerated by the Catch2 tests in `test/` (*test fixture*), and
legacy material no code in `src/`, `test/`, or `y3_buzzard/` references
any more (*retired/unused*). Note that `data/.gitignore` contains
`*.out`, so every `.out` file below is a gitignored test-run diagnostic
dump, not tracked reference data; several tests have since moved their
dumps to `validation-data/`, leaving stale `.out` copies here.

### Lensing / ΔΣ / $\Sigma_{\rm crit}$

| Path | Quantity | Class | Used by | Notes |
|---|---|---|---|---|
| `data/test_cluster_Y1.fits` | DES Y1 catalog + source $n(z)$ (`nz_source` HDU: `z_mid`, `bin1`) | pipeline input | `average_sigma_crit_inv.py` (HDU 6); also legacy `sigma_park_y1/buildSigmaCritInv.py` | Source p(z) for $\langle\Sigma_{\rm crit}^{-1}\rangle(z_l)$; bypassed when the module runs with `unity = T` |
| `data/sigma_crit_inv/` (`zl.txt`, `zs.txt`, `z_c0.txt`, `d_a_c0.txt`, `sigma_crit_inverse_c0.txt`) | astropy reference $\Sigma_{\rm crit}^{-1}(z_l, z_s)$ grid | test fixture | `test/sigma_crit_inv.test.cc` | `z_c1.txt` unused (`NCOSMO = 1`); `sigma_crit_inverse.py` is the generator script |
| `data/deltasigma_test/` (`Sigma_nfwonly.txt`, `Sigma_mm_2halo.txt`, `xi_nfwonly.txt`, `xi_mm_2halo.txt`, `bias.txt`) | NFW-only and 2-halo $\Sigma$/$\xi$ reference tables | retired/unused | — | Fixtures for a deleted ΔΣ test; `generate_test_files.py` is the generator |
| `data/DeltaSigma_for_Maria/` (`DS_M_*_z_*_cosi_*.dat`) | $\Delta\Sigma(R)$ in mass/z/$\cos i$ bins | retired/unused | — | One-off hand-off data product, never read by code |
| `data/deltasigma_{1,2,bias,m_h,r_perp,z}.txt` | tabulated ΔΣ grids (SDSS era) | retired/unused | — | `del_sig_tom.hh`/`del_sig_sum.hh` read datablock section `deltasigma`, not these files |
| `data/Sigma_crit_test.out` | $\Sigma_{\rm crit}$ regression dump | retired/unused | — | `sigma_crit_inv.test.cc` now writes `validation-data/Sigma_crit_test.out`; stale copy |
| `data/beta_sigCrit_lut_mock_buzzard_zl.npz` | $\langle\beta\rangle$/$\Sigma_{\rm crit}$ LUT for the Buzzard mock | retired/unused | `y3_buzzard/buildSigmaCritInv.py`, `y3_buzzard/buzzard.ini` | Only the local Buzzard scratch harness touches it, not the reference pipeline |

### Miscentering (nfw_off_center + ROFFSET)

| Path | Quantity | Class | Used by | Notes |
|---|---|---|---|---|
| `data/nfw_off_center/table_1000_1e-03_5e+03_gamma_logx.txt`, `..._gamma_logxmis.txt`, `..._log_deltasigma_gamma.txt` | γ-kernel miscentered NFW $\Delta\Sigma_{\rm mis}$ lookup ($1000\times1000$, log axes $x = R/r_s$, $x_{\rm mis}$) | pipeline input | `NFW_DSIGMA_MIS` default (GAMMA) ctor in `src/models/nfw_dsigma_mis.hh`, constructed by `Shear1hMisSel` via `lensing_weights.hh`; also `test/nfw_dsigma_mis.test.cu` | Paths resolved relative to `$Y3_CLUSTER_CPP_DIR/data/` via `read_vector` |
| `data/nfw_off_center/table_1000_1e-03_5e+03_single_logx.txt`, `..._single_logxmis.txt`, `..._log_sigma_single.txt`, `..._log_deltasigma_single.txt` | single-offset (δ) kernel $\Sigma_{\rm mis}$ / $\Delta\Sigma_{\rm mis}$ lookups | pipeline input | `shear_prj` (`ShearPrjEvaluator`): `sigma_prj_t.hh` constructs `NFW_SIGMA_MIS`/`NFW_DSIGMA_MIS` with the `SINGLE` kernel | Inner miscentered NFW of the two-halo projection integrand |
| `data/nfw_off_center/table_1000_1e-03_5e+03_log_sigma_gamma.txt` | γ-kernel $\Sigma_{\rm mis}$ lookup | test fixture | `test/nfw_sigma_mis.test.cu` | Not on the current pipeline path (pipeline uses the `single` Σ table) |
| `data/test_Rmis.txt` | $R_{\rm mis}$ Γ-distribution reference values | test fixture | `test/roffset_t.test.cc` | |
| `data/Rmis_gammadist_test.out`, `data/nfw_sigma_mis_test.out`, `data/nfw_dsigma_mis_test.out` | test diagnostic dumps | test fixture | written by `roffset_t` / `nfw_*_mis` tests | Gitignored outputs |

### HMF / mass grids

| Path | Quantity | Class | Used by | Notes |
|---|---|---|---|---|
| `data/m_h_matteo.txt`, `data/z_matteo.txt`, `data/dndlnmh_matteo.txt` | reference $dn/d\ln M(M, z)$ grid | test fixture | `test/hmf_t.test.cc` | Mass axis shifted by $\ln\Omega_m$ inside the test |
| `data/m_h.txt`, `data/dndlnmh.txt` | large legacy $dn/d\ln M$ grid (12 MB) | retired/unused | — | Superseded; runtime HMF comes from datablock `mass_function/dndlnmh` (MfTinker) |
| `data/hmf_test.out`, `data/masses_test.out` | HMF test dumps | test fixture / retired | `hmf_t.test.cc` | Gitignored diagnostics |
| `data/tinker_bias_function/` (`ln_mass_h.txt`, `z.txt`, `bias.txt`) | Tinker $b(M,z)$ table | retired/unused | only the legacy `HMB_t` default ctor | Halo bias now comes from datablock `halomodel/bias` written by `halo_model_cosmosis.py`; no test or pipeline module loads these files |

### Richness kernels (lc_lt / lo_lc / P(ξ|ζ))

| Path | Quantity | Class | Used by | Notes |
|---|---|---|---|---|
| `data/test_lc_lt_t_SDSS.txt` | SDSS $P(\lambda_{\rm true}\,\vert\,M,z)$ reference values | test fixture | `test/lc_lt_t.test.cc` | `lc_lt_test.out` is a stale dump |
| `data/test_lolc_t.txt` | $P(\lambda_{\rm ob}\,\vert\,\lambda_{\rm true})$ reference values | test fixture | `test/lo_lc_t.test.cc` | |
| `data/int_lc_lt_des_t_lt_bins.txt`, `data/int_lc_lt_des_t_zt_bins.txt` | DES ∫lc_lt fixture axes ($\lambda_t$, $z_t$ nodes) | test fixture | `test/int_lc_lt_des_t_test.test.cu`, `test/int_lc_lt_des_f_test.test.cu` | GPU tests; `int_lc_lt_des_t.out` is their diagnostic dump |
| `data/int_lc_lt_des_t_lbd_bin{0..3}.txt` | per-λ-bin reference answers | test fixture | answer-generation recipe in `int_lc_lt_des_f_test.test.cu` | Provenance of the hard-coded `answers` array; not read at test runtime |
| `data/test_pxizeta_t.txt`, `data/test_int_pxizeta.txt` | SPT $P(\xi\,\vert\,\zeta)$ kernel references | test fixture | `test/pxizeta_t.test.cc`, `test/int_pxizeta.test.cc` | |
| `data/test_ln_mez_power_law.txt` | $\ln M(\xi, z)$ power-law reference | test fixture | `test/ln_mez_power_law.test.cc` | |
| `data/lc_lt_fits/DESY1A_v1.4/` (`lamtrue.txt`, `z.txt`, `mu.txt`, `sigma.txt`, `tau.txt`, `fprj.txt`, `fmsk.txt`) | DES Y1A EMG richness-kernel fit tables vs $(\lambda_{\rm true}, z)$ | test fixture | `src/models/lc_lt_y1_t.cc` (`LC_LT_Y1_t`), DES lc_lt GPU tests and legacy modules | Not on the reference path: the Costanzi-2026 EMG coefficients are embedded in `y3_buzzard/prj_params.py` |
| `data/lc_lt_params.txt` | SDSS-era lc_lt spline parameters | retired/unused | — | |
| `data/prj_params/` | (empty) | retired/unused | — | EMG coefficient table moved into `y3_buzzard/prj_params.py` |

### MOR (mass–observable relation)

| Path | Quantity | Class | Used by | Notes |
|---|---|---|---|---|
| `data/mor_tt_test.txt` | MOR reference values | test fixture | `test/mor_t2.test.cc` | |
| `data/test_sptxdes_mor_1d.txt` | SPT×DES 1-D MOR reference | test fixture | `test/sptxdes_mor_1d.test.cc` | |
| `data/mass_conversion/` (`lnm200m_*.txt`, `z_200m_*.txt`, `lnm500c_*.txt`) | $M_{200m}\!\to\!M_{500c}$ Interp2D grid (Child+18 concentration) | test fixture | `test/mor_y3xspt_t.test.cc` | At runtime `MOR_Y3XSPT_t` reads the equivalent table from datablock `mass_conversion/*` |

### Photo-z

| Path | Quantity | Class | Used by | Notes |
|---|---|---|---|---|
| `data/test_zo_zt_des.txt`, `data/test_int_zo_zt_des.txt` | DES $P(z_{\rm ob}\,\vert\,z_{\rm true})$ references | test fixture | `test/zo_zt_des_t.test.cc`, `test/int_zo_zt_des_t.test.cc` | |
| `data/p_z.txt`, `data/z_s.txt` | legacy source $n(z)$ table | retired/unused | — | Superseded by `test_cluster_Y1.fits` |
| `data/z_kernel/` | (empty) | retired/unused | — | The 120-node photo-z width table $\sigma(z)$ was embedded bit-exactly into `src/models/z_kernel_data.hh`; no disk access remains |

### Geometry / volume / distances

| Path | Quantity | Class | Used by | Notes |
|---|---|---|---|---|
| `data/distances/` (`z.txt`, `d_a.txt`, `d_m.txt`) | $d_A(z)$, $d_M(z)$ fixture grid | test fixture | `src/models/sample_variance.hh`, `test/sample_variance.cc`, `test/bessel_integral_equivalence.test.cc` | |
| `data/matter_power_lin/` (`z.txt`, `k_h.txt`, `p_k.txt`) | linear $P(k, z)$ fixture grid | test fixture | `src/models/sample_variance.hh`, `test/sample_variance.cc` | Distinct from the datablock section of the same name written by cp_camb |
| `data/z_da_test.txt`, `data/d_a_test.txt` | $d_A(z)$ fixture | test fixture | `test/dv_do_dz_t.test.cc`, sample-variance tests | |
| `data/test_dv_do_dz.txt` | $dV/d\Omega dz$ reference values | test fixture | `test/dv_do_dz_t.test.cc` | |
| `data/omega_z_sdss.txt` | SDSS survey area $\Omega(z)$ | test fixture | `test/omega_z_sdss.test.cc` | |
| `data/test_omega_z_y3xspt.txt` | Y3×SPT $\Omega(z)$ reference | test fixture | `test/omega_z_y3xspt.test.cc` | |
| `data/d_a.txt`, `data/z_da.txt`, `data/z.txt` | legacy distance grids | retired/unused | — | Only the `*_test.txt` variants are read |

### Regression / harness fixtures (misc)

| Path | Quantity | Class | Used by | Notes |
|---|---|---|---|---|
| `data/test_grid_file.txt` | 3-row grid-file loader fixture | test fixture | `test/make_grid_points.test.cc` | |
| `data/interp_1d.out` | interpolation test dump | test fixture | written by `test/interp_1d.test.cc`, `test/cuda_interp_1d.test.cu` | Gitignored diagnostic |
| `data/integrated_bin_test.out`, `data/integrated_bin_y1_test.out` | legacy CosmoSIS-harness dumps | retired/unused | — | |

### SZ (DES×SPT Compton-y)

| Path | Quantity | Class | Used by | Notes |
|---|---|---|---|---|
| `data/sz_mock/` (12 `mass_*_z_*.fits`, ini files, `plot.py`, `README.txt`) | mock Compton-y data vector per $(M, z)$ bin | retired/unused | — | MCMC recovery test for the retired `compton_y_sims` module |
| `data/sz_sims/` (12 `mass_*_z_*.fits`) | simulation-derived Compton-y profiles | retired/unused | — | Companion to `sz_mock` |

### Mock data vector (`data/mock/`)

| Path | Quantity | Class | Used by | Notes |
|---|---|---|---|---|
| `data/mock/mock_dv_cp_camb.npz` | mock data vector (counts + 1h shear + projected shear) with covariance | pipeline input | `[likelihoods]` → `y3_buzzard/likelihood_cp.py` | **`data/mock/` is not present in the current tree**; the `.npz` is built by the mock-DV scripts in `$PSCRATCH/github/des-cluster-nersc` and must exist before an MCMC run |
